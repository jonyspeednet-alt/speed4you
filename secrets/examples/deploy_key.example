# SSH deploy key — TEMPLATE
#
# This is a placeholder. Real keys are NEVER committed.
# Place your real key at ***REMOVED***s/local/deploy_key (no extension).
#
# To generate a new one:
#   ssh-keygen -t ed25519 -f ***REMOVED***s/local/deploy_key -N "" -C "deploy@speed4you"
#
# Then add ***REMOVED***s/local/deploy_key.pub to:
#   - ~/.ssh/authorized_keys on the deploy server
#   - GitHub repository ***REMOVED***s as DEPLOY_SSH_KEY (private half)

***REMOVED***
THIS_IS_A_PLACEHOLDER_DO_NOT_USE_THIS_KEY
REPLACE_WITH_THE_OUTPUT_OF_ssh-keygen
-----END OPENSSH PRIVATE KEY-----

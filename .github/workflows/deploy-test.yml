name: Deploy Test
on:
  push:
    branches: [main]
jobs:
  test:
    runs-on: self-hosted
    steps:
      - name: Checkout
        uses: actions/checkout@v5
      - run: ls -la && echo "Checkout works"